#!/bin/sh
# rebuilds files generated by autoconf&automake
# arguments are forwarded to the autoreconf script
cd `dirname $0`
{ autoreconf --foreign -l config "$@" && touch config/stamp-h.in; } ||
{ echo "please apply patches/autoreconf to GNU autoconf's autoreconf script" &&
  exit 1; }
