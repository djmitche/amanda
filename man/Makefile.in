# Makefile.in generated automatically by automake 1.1o from Makefile.am

# Copyright (C) 1994, 1995, 1996 Free Software Foundation, Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.


SHELL = /bin/sh

srcdir = @srcdir@
top_srcdir = @top_srcdir@
VPATH = @srcdir@
prefix = @prefix@
exec_prefix = @exec_prefix@

bindir = @bindir@
sbindir = @sbindir@
libexecdir = @libexecdir@
datadir = @datadir@
sysconfdir = @sysconfdir@
sharedstatedir = @sharedstatedir@
localstatedir = @localstatedir@
libdir = @libdir@
infodir = @infodir@
mandir = @mandir@
includedir = @includedir@
oldincludedir = /usr/include

pkgdatadir = $(datadir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@

top_builddir = ..

ACLOCAL = @ACLOCAL@
AUTOCONF = @AUTOCONF@
AUTOMAKE = @AUTOMAKE@
AUTOHEADER = @AUTOHEADER@

INSTALL = @INSTALL@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_SCRIPT = @INSTALL_SCRIPT@

NORMAL_INSTALL = true
PRE_INSTALL = true
POST_INSTALL = true
NORMAL_UNINSTALL = true
PRE_UNINSTALL = true
POST_UNINSTALL = true
build_alias = @build_alias@
build_triplet = @build@
host_alias = @host_alias@
host_triplet = @host@
target_alias = @target_alias@
target_triplet = @target@
ACLOCAL = @ACLOCAL@
AMANDA_CFLAGS = @AMANDA_CFLAGS@
AMPLOT_CAT_COMPRESS = @AMPLOT_CAT_COMPRESS@
AMPLOT_CAT_GZIP = @AMPLOT_CAT_GZIP@
AMPLOT_CAT_PACK = @AMPLOT_CAT_PACK@
AMPLOT_COMPRESS = @AMPLOT_COMPRESS@
AR = @AR@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
AWK_VAR_ASSIGNMENT_OPT = @AWK_VAR_ASSIGNMENT_OPT@
BUILD_AMPLOT_LIBEXEC = @BUILD_AMPLOT_LIBEXEC@
BUILD_AMPLOT_SBIN = @BUILD_AMPLOT_SBIN@
BUILD_CHANGER_PROGS_LIBEXEC = @BUILD_CHANGER_PROGS_LIBEXEC@
BUILD_CHANGER_SCRIPTS_LIBEXEC = @BUILD_CHANGER_SCRIPTS_LIBEXEC@
BUILD_CLIENT_PROGS_LIBEXEC = @BUILD_CLIENT_PROGS_LIBEXEC@
BUILD_CLIENT_SCRIPTS_LIBEXEC = @BUILD_CLIENT_SCRIPTS_LIBEXEC@
BUILD_CLIENT_SCRIPTS_SBIN = @BUILD_CLIENT_SCRIPTS_SBIN@
BUILD_SERVER_PROGS_LIBEXEC = @BUILD_SERVER_PROGS_LIBEXEC@
BUILD_SERVER_PROGS_SBIN = @BUILD_SERVER_PROGS_SBIN@
BUILD_SERVER_SCRIPTS_SBIN = @BUILD_SERVER_SCRIPTS_SBIN@
CAT = @CAT@
CC = @CC@
CLIENT_LOGIN = @CLIENT_LOGIN@
COMPRESS = @COMPRESS@
CONFIG_DIR = @CONFIG_DIR@
DB_DIR = @DB_DIR@
DD = @DD@
DEFAULT_CONFIG = @DEFAULT_CONFIG@
DEFAULT_SERVER = @DEFAULT_SERVER@
DEFAULT_TAPE_DEVICE = @DEFAULT_TAPE_DEVICE@
DEFAULT_TAPE_SERVER = @DEFAULT_TAPE_SERVER@
DUMP = @DUMP@
EGREP = @EGREP@
GNUPLOT = @GNUPLOT@
GNUTAR = @GNUTAR@
GREP = @GREP@
GZIP = @GZIP@
INDEX_DIR = @INDEX_DIR@
KRB4CLIENT_C = @KRB4CLIENT_C@
KRB4CLIENT_O = @KRB4CLIENT_O@
KRB4COMMON_C = @KRB4COMMON_C@
KRB4COMMON_O = @KRB4COMMON_O@
LEX = @LEX@
LOG_DIR = @LOG_DIR@
MAILER = @MAILER@
MAKEINFO = @MAKEINFO@
MT = @MT@
MTX = @MTX@
MT_FILE_FLAG = @MT_FILE_FLAG@
PACKAGE = @PACKAGE@
PCAT = @PCAT@
PERL = @PERL@
RANLIB = @RANLIB@
RESTORE = @RESTORE@
SAMBA_CLIENT = @SAMBA_CLIENT@
SETUID_GROUP = @SETUID_GROUP@
SHELL = @SHELL@
TAPE_NO_REWIND_DEVICE = @TAPE_NO_REWIND_DEVICE@
TAPE_REWIND_DEVICE = @TAPE_REWIND_DEVICE@
USE_VERSION_SUFFIXES = @USE_VERSION_SUFFIXES@
VERSION = @VERSION@
VERSION_COMMENT = @VERSION_COMMENT@
VERSION_MAJOR = @VERSION_MAJOR@
VERSION_MINOR = @VERSION_MINOR@
VERSION_PATCH = @VERSION_PATCH@
VERSION_SUFFIX = @VERSION_SUFFIX@
XFSDUMP = @XFSDUMP@
XFSRESTORE = @XFSRESTORE@
YACC = @YACC@
ac_c = @ac_c@
ac_n = @ac_n@

transform =	s,x,x,;

man_MANS =	amadmin.8	amanda.8	amcheck.8	\
		amcleanup.8	amdump.8	amflush.8	\
		amlabel.8	amplot.8	amrecover.8	\
		amrestore.8	amrmtape.8	amtape.8	\
		amtoc.8
mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
CONFIG_HEADER = ../config.h
CONFIG_CLEAN_FILES =  amadmin.8 amanda.8 amcheck.8 amcleanup.8 amdump.8 \
amflush.8 amlabel.8
MANS = amadmin.8	amanda.8	amcheck.8			amcleanup.8	amdump.8	amflush.8			amlabel.8	amplot.8	amrecover.8			amrestore.8	amrmtape.8	amtape.8			amtoc.8


NROFF = nroff
DIST_COMMON =  Makefile.am Makefile.in amadmin.8.in amanda.8.in \
amcheck.8.in amcleanup.8.in amdump.8.in amflush.8.in amlabel.8.in


DISTFILES = $(DIST_COMMON) $(SOURCES) $(HEADERS) $(TEXINFOS) $(EXTRA_DIST)

TAR = tar
default: all

.SUFFIXES:
$(srcdir)/Makefile.in: Makefile.am $(top_srcdir)/configure.in $(ACLOCAL_M4) 
	cd $(top_srcdir) && $(AUTOMAKE) --foreign man/Makefile

Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status $(BUILT_SOURCES)
	cd $(top_builddir) \
	  && CONFIG_FILES=$(subdir)/$@ CONFIG_HEADERS= $(SHELL) ./config.status

amadmin.8: $(top_builddir)/config.status amadmin.8.in
	cd $(top_builddir) && CONFIG_FILES=$(subdir)/$@ CONFIG_HEADERS= ./config.status
amanda.8: $(top_builddir)/config.status amanda.8.in
	cd $(top_builddir) && CONFIG_FILES=$(subdir)/$@ CONFIG_HEADERS= ./config.status
amcheck.8: $(top_builddir)/config.status amcheck.8.in
	cd $(top_builddir) && CONFIG_FILES=$(subdir)/$@ CONFIG_HEADERS= ./config.status
amcleanup.8: $(top_builddir)/config.status amcleanup.8.in
	cd $(top_builddir) && CONFIG_FILES=$(subdir)/$@ CONFIG_HEADERS= ./config.status
amdump.8: $(top_builddir)/config.status amdump.8.in
	cd $(top_builddir) && CONFIG_FILES=$(subdir)/$@ CONFIG_HEADERS= ./config.status
amflush.8: $(top_builddir)/config.status amflush.8.in
	cd $(top_builddir) && CONFIG_FILES=$(subdir)/$@ CONFIG_HEADERS= ./config.status
amlabel.8: $(top_builddir)/config.status amlabel.8.in
	cd $(top_builddir) && CONFIG_FILES=$(subdir)/$@ CONFIG_HEADERS= ./config.status
install-man: $(MANS)
	$(NORMAL_INSTALL)
	$(mkinstalldirs) $(mandir)/man8
	@sect=8;				\
	inst=`echo "amadmin" | sed '$(transform)'`.8; \
	if test -f $(srcdir)/amadmin.8; then file=$(srcdir)/amadmin.8; \
	else file=amadmin.8; fi; \
	echo " $(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst"; \
	$(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "amrestore" | sed '$(transform)'`.8; \
	if test -f $(srcdir)/amrestore.8; then file=$(srcdir)/amrestore.8; \
	else file=amrestore.8; fi; \
	echo " $(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst"; \
	$(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "amrmtape" | sed '$(transform)'`.8; \
	if test -f $(srcdir)/amrmtape.8; then file=$(srcdir)/amrmtape.8; \
	else file=amrmtape.8; fi; \
	echo " $(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst"; \
	$(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "amtape" | sed '$(transform)'`.8; \
	if test -f $(srcdir)/amtape.8; then file=$(srcdir)/amtape.8; \
	else file=amtape.8; fi; \
	echo " $(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst"; \
	$(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "amtoc" | sed '$(transform)'`.8; \
	if test -f $(srcdir)/amtoc.8; then file=$(srcdir)/amtoc.8; \
	else file=amtoc.8; fi; \
	echo " $(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst"; \
	$(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "amanda" | sed '$(transform)'`.8; \
	if test -f $(srcdir)/amanda.8; then file=$(srcdir)/amanda.8; \
	else file=amanda.8; fi; \
	echo " $(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst"; \
	$(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "amcheck" | sed '$(transform)'`.8; \
	if test -f $(srcdir)/amcheck.8; then file=$(srcdir)/amcheck.8; \
	else file=amcheck.8; fi; \
	echo " $(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst"; \
	$(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "amcleanup" | sed '$(transform)'`.8; \
	if test -f $(srcdir)/amcleanup.8; then file=$(srcdir)/amcleanup.8; \
	else file=amcleanup.8; fi; \
	echo " $(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst"; \
	$(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "amdump" | sed '$(transform)'`.8; \
	if test -f $(srcdir)/amdump.8; then file=$(srcdir)/amdump.8; \
	else file=amdump.8; fi; \
	echo " $(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst"; \
	$(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "amflush" | sed '$(transform)'`.8; \
	if test -f $(srcdir)/amflush.8; then file=$(srcdir)/amflush.8; \
	else file=amflush.8; fi; \
	echo " $(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst"; \
	$(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "amlabel" | sed '$(transform)'`.8; \
	if test -f $(srcdir)/amlabel.8; then file=$(srcdir)/amlabel.8; \
	else file=amlabel.8; fi; \
	echo " $(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst"; \
	$(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "amplot" | sed '$(transform)'`.8; \
	if test -f $(srcdir)/amplot.8; then file=$(srcdir)/amplot.8; \
	else file=amplot.8; fi; \
	echo " $(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst"; \
	$(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "amrecover" | sed '$(transform)'`.8; \
	if test -f $(srcdir)/amrecover.8; then file=$(srcdir)/amrecover.8; \
	else file=amrecover.8; fi; \
	echo " $(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst"; \
	$(INSTALL_DATA) $$file $(mandir)/man$$sect/$$inst

uninstall-man:
	$(NORMAL_UNINSTALL)
	inst=`echo "amadmin" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "amrestore" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "amrmtape" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "amtape" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "amtoc" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "amanda" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "amcheck" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "amcleanup" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "amdump" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "amflush" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "amlabel" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "amplot" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "amrecover" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst

tags: TAGS
TAGS:


distdir = $(top_builddir)/$(PACKAGE)-$(VERSION)/$(subdir)

subdir = man
distdir: $(DISTFILES)
	@for file in $(DISTFILES); do \
	  d=$(srcdir); \
	  test -f $(distdir)/$$file \
	  || ln $$d/$$file $(distdir)/$$file 2> /dev/null \
	  || cp -p $$d/$$file $(distdir)/$$file; \
	done
info:
dvi:
check: all
	$(MAKE)
installcheck:
install-exec: 
	@$(NORMAL_INSTALL)

install-data: install-man
	@$(NORMAL_INSTALL)
	$(MAKE) install-data-hook

install: install-exec install-data all
	@:

uninstall: uninstall-man

all: Makefile $(MANS)

install-strip:
	$(MAKE) INSTALL_PROGRAM='$(INSTALL_PROGRAM) -s' install
installdirs:
	$(mkinstalldirs)  $(mandir)/man8


mostlyclean-generic:
	test -z "$(MOSTLYCLEANFILES)" || rm -f $(MOSTLYCLEANFILES)

clean-generic:
	test -z "$(CLEANFILES)" || rm -f $(CLEANFILES)

distclean-generic:
	rm -f Makefile $(DISTCLEANFILES)
	rm -f config.cache config.log stamp-h
	test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)

maintainer-clean-generic:
	test -z "$(MAINTAINERCLEANFILES)" || rm -f $(MAINTAINERCLEANFILES)
	test -z "$(BUILT_SOURCES)" || rm -f $(BUILT_SOURCES)
mostlyclean:  mostlyclean-generic

clean:  clean-generic mostlyclean

distclean:  distclean-generic clean
	rm -f config.status

maintainer-clean:  maintainer-clean-generic distclean
	@echo "This command is intended for maintainers to use;"
	@echo "it deletes files that may require special tools to rebuild."

.PHONY: default install-man uninstall-man tags distdir info dvi \
installcheck install-exec install-data install uninstall all \
installdirs mostlyclean-generic distclean-generic clean-generic \
maintainer-clean-generic clean mostlyclean distclean maintainer-clean


install-data-hook:
	@list="$(man_MANS)"; echo $$list; for p in $$list; do \
		pa=$(mandir)/man8/`echo $$p|sed '$(transform)'`; \
		echo chown $(CLIENT_LOGIN) $$pa; \
		chown $(CLIENT_LOGIN) $$pa; \
		echo chgrp $(SETUID_GROUP) $$pa; \
		chgrp $(SETUID_GROUP) $$pa; \
	done

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
